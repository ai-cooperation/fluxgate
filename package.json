{
  "name": "fluxgate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail --format=pretty"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
