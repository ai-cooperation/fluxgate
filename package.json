{
  "name": "fluxgate",
  "version": "0.1.0",
  "description": "Cloudflare Workers AI gateway for intent-to-FLUX image generation with REST and MCP endpoints.",
  "private": false,
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ai-cooperation/fluxgate.git"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "issue-key": "node scripts/issue-key.mjs",
    "tail": "wrangler tail --format=pretty",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
